# horcom C++ Architecture

The rewrite plan, derived from the four legacy analyses in `docs/legacy/`. Read those first for any subsystem detail; this document decides how the new program is built.

## 1. What HORCOM is, in one paragraph

An event-driven chart calculator. Input (date, time, place) is normalised to UT through zone/DST/LMT logic, converted to Julian date and then ET via a ΔT model. Planet positions come from three engines: truncated VSOP87 series (Sun through Neptune, from a binary term table), a Meeus Moon theory, and self-integrated binary ephemerides for Pluto, Chiron, the centaurs, the big asteroids, Quaoar, Eris and Halley. Positions pass through light-time/aberration, equatorial conversion and (optionally) Robert Rettig's topocentric parallax correction. Houses (7 systems), aspects (harmonic scan with per-body orbs), midpoints, mundane positions and a dozen special factors complete the chart, which is rendered on a 640×480 virtual canvas and dispatched through one universal output-screen driver with ~60 menu features.

## 2. Module layout

```
src/
  core/     Constants and angle plumbing. funkt constants (pu, up, puu, tja, kk),
            normalisation nb/ng/nh/wz, the atn quadrant fix, the vergl1/vergl2
            branch reconcilers. Ported verbatim; everything depends on their
            exact branch behaviour.
  time/     Julian date <-> calendar with the Julian/Gregorian override (juld/dat),
            time arguments t1..t41 (juld1), delta T (utet), sidereal time (sidt),
            equation of time (zeitgleichung), zone/DST/LMT input normalisation.
  ephem/    Position engines. VSOP term-table loader and evaluator
            (readpterm/plposhi), Moon (moko1/somo), Kepler solver, mean elements
            (plelem), Chapront Pluto fallback (pl_ko), the .EPH reader
            (ephem_auswert + ipol/ipol3), nutation and obliquity (somo),
            precession (praez, praez_kartes_aeqn/ekln).
  chart/    The chart pipeline. Dispatcher (plko/plko_einz), helio->geo (hel_geo),
            apparent position (plkoap), PARALLAX (par) [protected, see 4],
            coordinate transforms (ko_tr1/ko_tr2), AC/MC/Vertex (eckp1), all house
            systems, aspects (asp0/asp1/asp11 + orbs), midpoints (halbs*), mundane
            positions (md/mundan), special factors (true node and Lilith from moko,
            Part of Fortune, Arabic parts, fixed stars, Grosses Jahr), transit and
            return root finding (plant).
  data/     Byte-exact loaders for the legacy formats (chart .DAT, places .INT,
            KONSTA7P settings, PLANETS.NDX/DAT, .EPH, AAF, statistics), per
            docs/legacy/data-formats.md, plus the port's own storage later
            (SQLite; AAF stays the interchange format).
  geo/      Places gazetteer and the historical timezone data. The 177-entry
            ZEITZONN table (with pre-1900 local mean times) becomes seed data;
            IANA tzdata covers the modern era.
  render/   Chart geometry as data: ring radii, polar mapping, de-clumping,
            colours and draw order per docs/legacy/ui-and-graphics.md. Produces a
            backend-neutral display list so screen, PNG/SVG export and print are
            one code path.
  app/      The GUI shell, Qt 6 Widgets. The display list renderer feeds
            QPainter directly and the licence sits cleanly with the project.
            The rest of the tree never includes UI headers, the CLI front
            end came first for testing.
tests/      Golden-value tests against the original program plus unit tests.
```

Dependency direction is strictly downward: `app -> render -> chart -> ephem -> time -> core`, with `data` and `geo` as leaves used by `chart`/`app`. The calculation library must build and run headless.

## 3. Design decisions

1. **Radians and doubles everywhere.** The original computes in radians with GFA 8-byte floats. Rettig's own porting advice (EPHEMER5 header): time variables need at least 12 significant digits — `double` satisfies this. His exact constants are kept, including `tja = 365.24219878/9` and the `kk = 1e-10` epsilon where behaviour depends on it.
2. **Same numbers first, better structure second.** Every ported routine first reproduces the original's output (golden tests, see 5), then may be refactored under those tests. Algorithm upgrades (IAU 2006 precession, modern ΔT, Swiss-Ephemeris-grade positions) are explicitly out of scope for the port and become opt-in engine variants later.
3. **His data structures are kept where they carry the design.** The body index scheme 0..40 (0 Fixpunkt, 1 Sun … 10 Pluto, 11/12 nodes, 13/14 AC/MC, 19+ extra bodies via the `nk&` slot table) stays as a typed enum with identical numbering so every formula and file format reads literally. The chart-slot model (`od` 0..3 × `ze` 0..5) becomes a `ChartSet` type with the same shape. The int32-scaled 16-byte `.EPH` record stays the on-disk ephemeris format, read by a byte-exact loader — those files are his data legacy and remain valid.
4. **Verbatim-semantics kernel.** `nb/ng/nh`, `atn`, `vergl1/vergl2`, `ipol/ipol3` are ported instruction-for-instruction, tests pinning their edge cases, because aspects and interpolation depend on their exact branch behaviour.
5. **Comments travel with the code.** Per CLAUDE.md, Rettig's comments are carried into the ported routines in his German wording where they still apply, marked as his.
6. **Dropped subsystems** (decided, documented): the KENNTS copy protection and demo gates (contain personal data, no value), NOTIZ/AEND7 vestiges, the resolution-locked tiled BMP caches (regenerate instead), MSPAINT/clipboard export (native PNG/SVG export instead), the temp-file KILL/NAME edit pattern (atomic writes instead).
7. **Known original bugs** are fixed, never reproduced. Each fix follows proof against an independent reference and carries a test that pins the corrected value and names the original's value:
   - Fixed: velocity interpolation offset `yip = jdip − djd/2` in `ephem_auswert` mixed a step count with days, so the velocity was extrapolated 4.5, 9.5 or 19.5 steps away for the 10, 20 and 40 day files (Ceres speed off by up to 1.6'/day, Halley by 200'/day). Now `jdip − 0.5`.
   - Fixed: the heliocentric rates `helt`, `hert`, `hebt` were wrapped in `ABS`, so a falling radius, a southward latitude and Halley's retrograde longitude entered the geocentric speed with the wrong sign. Together with the offset above the R marker was wrong on 40 days per decade for Ceres and on 200 of 800 days around Halley's 1986 perihelion. Now signed.
   - Fixed: Pluto's file is equatorial, the right ascension rate was used as the ecliptic longitude rate. The rates now come from the state rotated into the ecliptic.
   - Fixed: `prima` converted the summed sidereal time variation with `dst = 0.0027379 // 1/tja` days per degree, one day over the year length, 3.94 minutes per degree against his own dialog text of 4 minutes. The port uses the sidereal rate, 1/(360 · 1.00273790935) days per degree.
   - Fixed: KORREKTUR by the Moon seeded `jd = jdx + tmo·(el(2) − pz)/2π` without folding the difference, a target across 0° Aries sent the search a whole lunation away. The difference is folded into ±180°.
   - Fixed: KORREKTUR by the Sun repaired a crossing in the wrong year only beyond 0.9 of a year, a target some 37° ahead of the natal Sun slipped a year early. The crossing nearest the birth is taken.
   - Fixed: the damped AC and cusp walk of `korr1` compared the wanted and found degree without folding, a target near 0° Aries threw the walk across the circle. The difference is folded into ±180°.
   - Fixed: the GRAD-LISTE midpoint pass tests `IF NOT (u& = w&) && NOT ((u& = 11 && w& = 12) OR …)`. GFA binds `NOT` looser than `&&`, so the line reads `NOT (u = w && NOT nodes)` and let the meaningless DR/DS midpoint through. The port skips the node pair as the line intends.
   - Fixed: the GRAD-LISTE never paired the Aries point. `a921` sets the four cardinal points to `po * (i& - 15) + kk`, lifting 0° Aries to exactly `kk`, and the pair guard of `grli` demands `pl > kk`, so every midpoint with AR dropped out while CN, LI and CP paired. The `+ kk` has no purpose other than surviving such an emptiness test, his list excludes all four by name as one family, and in the Hamburg school midpoint work the list serves the Aries point is the first of the four. The port pairs all four, SO at 10° now lists SO-AR at 5°, see `test_render.cpp`.
   - Fixed: the HALBSUMMEN-GRAPHIK (`aspar2`) weighed its cusp trees H2, H3, H5 and H6 with `or&(15..18)`. `orbis_pla` offers no row for them, its slot 15 is the ZUSATZ-PLANETEN row and 16 to 18 stay zero in every profile he shipped, so H3, H5 and H6 were drawn without a single branch and H2 followed the extra body weight. The trees weigh NORMAL = 100 % as his dialog caption names the neutral weight, see `test_aspects.cpp`.
   - Kept on purpose: `orbis_discr3` sets the midpoint orb from the pair alone and lets the third weight only gate participation. Both call sites carry his commented `//dds = MAX(o1,o2,o3) * dd` directly above the two weight call, the choice was his.
   - Kept on purpose: the house columns of the histogram leave AC and MC out, they stand exactly on the first and tenth cusp and would add the same two houses to every chart. The sign columns count them.
   - Kept on purpose: `halbs_dial`, the three column list of all midpoints, never opens. Its trigger reads `halbsmax%` while `halbsa` counts `halbsmax&`, two variables in GFA. His users always met the `halbs_zaehl_gr` popup instead, the one that can be switched off for the session, reviving the list would put an unskippable box after every chart with more than fifty midpoints. The rewrite writes those counts as a quiet line under the HALBSUMMEN-GRAPHIK, no popup at all. The midpoints stay readable in the HALBSUMMEN-GRAPHIK and the KOMPAKT-AUSWERTUNG.
   - Fixed: `kard_fix_gemh` doubled the first house planets and the birth ruler in the house quality column whatever PUNKTE im 1. HAUS DOPPELT and 1. GEBURTSHERRSCHER DOPPELT said, while `elem1`, `elem2` and `kard_fix_gem` ask both switches. The port asks them in every pass, see `test_histogram.cpp`.
   - Fixed: `mund1` unwrapped the three samples of each running body on its own, so a pair meeting across 0° Aries with only one body wrapping failed the bracket test. The new moon of 21 March 2023 at 0°49' Aries was missing from MUNDAN-ASPEKTE. The port brackets the folded difference of the pair, see `test_transit_search.cpp`.
   - Fixed: the void of course rule of `mund1` read the signs at the end of the three sample window, an aspect made shortly before a body left its sign dropped out. The port reads the signs at the moment of the aspect.
   - Fixed: `halbsmin` picked the near midpoint and then added π whenever the raw sum of the pair passed 2π and the result lay below π. Every pair around 0° Aries with such a sum got the far midpoint, 100° and 300° gave 200° instead of 20°. The composite planets, the statistics midpoint conditions and the midpoint targets of the direction and transit lists all read it. The port keeps the near midpoint, see `test_chart.cpp`.
   - Fixed: the direction window of `aprimout` held the arc in degrees against the asked Lebensjahre, under the NAIBOD key the list ran past the asked end (a ten degree arc listed at 10.15 years for a window ending at 10). The port holds the years, see `test_symbolic.cpp`.
   - Fixed: SIGNIFIKAT. OHNE Breite printed "Signifikator U. Promissor Ohne Breite" under the primary list while `aprim1` kept the significator's latitude. The port honours `bres&`.
   - Not a bug: the `ELSE drk!(w,t,u) = 0` of `aprimout` belongs to the inner mirror test, and the outer test already demands that flag to be clear, so it only clears a clear flag. The mirror check of the symbolic methods held and every symbolic arc listed once, as in the port.
   - Fixed: the midpoint pass `asy112` shared `drk!(w,t,u)` with the plain pass, the first midpoint of a factor silenced every other midpoint with that factor at the same multiple. The port keys the partner too.
   - Fixed: the intermediate cusps of the `ar_sys` Zwischenhäuser pass took the right ascension with the latitude of the last body of the main pass, `br` was never cleared. A cusp has no latitude in the port.
   - Fixed: `stat_ausw` searched IN ZEICHEN Widder with an orb from `w1 = ABS(wu)`, the orb before 0° Aries became a gap of the same size after it and a body at 2° Aries never matched. The port starts the window at 0° and the wrap pass covers the part before.
   - Fixed: under UND a window reaching over 0° Aries ran two passes, and `bed_erf` cleared the survivor of every record the running pass missed, so each pass wiped what the other had kept and no record ever survived. The port keeps a record when any pass keeps it, see `test_statist_eval.cpp`.
   - Fixed: the COMPOSIT mode MITTLERE STZ averaged the two sidereal times linearly, `hs = (hsi + hsa) / 2`, and added the mean longitude, the linear mean of the two local sidereal times. Two local sidereal times more than twelve hours apart gave the far half of the day, the ARMC stood opposite the MC midpoint and the tenth cusp fell on its IC. Both at 6 h Greenwich time at 170° east and west have local ARMCs of 260° and 280°, he gave 90°. The port takes the near mean of the two local ARMCs, see `test_chart.cpp`. A first version of this fix wrapped the Greenwich times instead and put the pair 1 h and 23 h at ±170° to 0° where 180° is right.
   - Fixed: AUFGANG printed its times as `jde + m − ΔT` through `etut`, though `jde` is 0h UT and Meeus gives the event at 0h UT plus m. The times came out early by ΔT, about a minute today and hours in antiquity. The port prints `jde + m`.
   - Fixed: AUFGANG interpolated at `n = m + ΔT` like Meeus for positions tabulated at 0h TD, while its three positions stand at 0h UT after the chain's own conversion. ΔT counted twice, the moon's clock moved by seconds today and by hours in antiquity. The port interpolates at `m`.
   - Fixed: the place file passes tested the longitude alone. `a2f_tr_ort` dropped every place with `ABS(lon) <= kk` and `a22ort` refused to enter one with a zero longitude or latitude, so Greenwich and every place on the equator vanished on TRIMMEN and could not be stored. The port treats a place as empty only when both coordinates are zero, like his Daten-Datei trim, see `test_data.cpp`.
   - Fixed: `ortp` wrote the VORZUGSORT as `STR$(CINT(gl*1000000),8,0)`, nine bytes west of −9.999999°, so no place in the Americas fit the eight byte field. The port writes a decimal there, which the reader already accepts.
   - Fixed: `aspdis` named the matched angle of the Aspektarium by dividing it down with FIX and matching truncated degree values, so 5/11 (163.6°) showed as the ninth, and with sixteen divisors 4/13 as the tenth, 5/14 and 4/15 as the eleventh. The Rhythmenlehre chain reads the same routine but only its main aspects, which never hit this. The port names the family of the reduced fraction, see `test_aspects.cpp`.
   - Fixed: STATISTIK UND after an aspect condition. `bed_erf_asp1` and `bed_erf_asp2` cleared a record only when an earlier hit of the same multiple had set its flag in that record, so a record without any aspect survived and a conjunction record was wiped by the next divisor's miss, the survivors came out inverted. KOMMSTAT says UND keeps the records that satisfy every earlier condition. The port keeps a record when any divisor hits and clears it otherwise, see `test_statist_eval.cpp`.
   - Fixed: STATISTIK SO/MO/AC and ALLE PLANETEN under UND. `bed_erf` cleared the record on the miss of any single body and wrote the mask at the list index, so the group acted as SO AND MO AND AC where KOMMSTAT describes an OR. The port keeps a record when any body of the group matches and writes its mask.
   - Fixed: STATISTIK LAGE Bei PLANET built its window as `kk - orb + target` without normalising, and `bed_erf` demands `w1 > 0`, so a target within the orb of 0° Aries never matched. The port normalises both ends and folds the window like the other passes.
   - Fixed: `stelk`, the aspect check of FIX-STERN-POSITIONEN and the ARABISCHE TEILE, opened the conjunction window on both sides of exact but the opposition, square and trine windows only on the side before it, `0 < nb(la + kπ - p) < orb`. An opposition half a degree past exact went unseen, half a degree before it was listed. The port opens every window both ways, see `test_chart.cpp`.
   - Fixed: `vel_om_pd` chose between `vergl2` and `vergl2r` with `IF w2 > w1 OR w2 < w1 + PI`, which always holds, so the retrograde fold never ran. Within an hour of the true node's backward step over 0° Aries, and of a retrograde true apogee there, the speed jumped by a full circle, about +4320° a day, with the wrong sign and R flag. The port brings the three samples next to the middle one in either direction, see `test_chart.cpp`.
   - Fixed: WINKEL-UMRECHNUNG (`rechne1`) took `ABS` on the way from decimal degrees to degree minute second and dropped the sign, -12.5 came out as 12 30 00, and on the way back added positive minutes to a negative degree, -12 30 read as -11.5. The port carries the sign both ways and the rounded hundredths carry into minutes and degrees, see `test_convert.cpp`.
   - Fixed: the ET minus UT line of `etm_ut` printed `TRUNC(dt * 24)`, the hours of the whole difference, so a delta T beyond a day read as 1d 29h, and its rounded seconds could read 60. The port splits days, hours, minutes and seconds, see `test_convert.cpp`.
   - Fixed: `et_ut_erl` set its remark only after 1995, before 948 and between 1620 and 1995, the years in between showed the remark of the previous run. The port leaves them empty.
   - Fixed: `labr2` accepted the coordinate box when only one of the two letters was valid, `NOT(E/W OR N/S)` instead of `NOT(E/W AND N/S)`. The port asks again until both hold.
   - Fixed: `input_grmise_ekl_aeq` read a latitude as `g + m / 60 + s / 3600`, a negative degree with positive minutes came out too small. The port signs the whole value by the letter or the degree. His `WINKEL >180°` box on an ecliptic longitude, whose retry he had commented out, is gone, longitudes run to 360°.
   - Fixed: the clock texts `homise$` with decimals and `ut_etd$` rounded their seconds without a carry and could print 60. The port rounds the moment first.
   - Fixed: his WCAPITAL.INT, the metropolis list of ORT-WANDERN F10, carried eight wrong records. Reykjavik stood at 21.93° east (it lies west), Kindley AFB on Bermuda at 33.37° N (32.37°), DENVER at 76.13° W 40.23° N near Harrisburg (104.98° W 39.73° N), PETROGRAD at 36.25° E (30.32° E), Mogadiscio at 49.32° E (45.32° E), the second HONGKONG record at 25.25° N (22.3° N), HELSINKI at 60.83° N (60.17° N) and KISANGANI at 15.23° E 0.43° S (25.18° E 0.52° N). The corrected file ships as `data/spez_ort/wcapital.int`, see `test_wander.cpp`.
   - Fixed: `auswert_ortelist` searched the nearest metropolis with the cosine of the walked latitude and printed the distance with the capital's, did not fold the longitude difference across the date line, and when ESC broke its first pass the second fell through to the last record. The port keeps one pass with the walked latitude and a folded difference.
   - Fixed: the aspect counter of ZEIT-WANDERN and STATISTIK kept Troika and GrTrig in the cells 14 and 15 of the per divisor array and summed 1 to 15 for its percentages, so with sixteen divisors the harmonics 14 and 15 overwrote them and 16 was left out. The port keeps them apart and sums every divisor with both.
   - Fixed: the MITTEL line of the DYNAMOGRAMM (`hubausg`) summed the samples 0 to 6000 into the integer `mittel%`, while the amplitude factor 0.3 had only been applied to the visible window 3000 to 3598. The samples outside the window entered unscaled, every addition was truncated, and `mittel%` was not cleared before a fresh run. The port scales every sample, sums in double and starts afresh, see `test_transit_search.cpp`.
   - Fixed: the regressive radix pass of the DYNAMOGRAMM called `asp_analy_rad(ljaAU)`, an unset variable, instead of `lja`. The value only reached the single arc screen, which then labelled the regressive arcs with the first year of life 0 and a scale from -2. The port passes the asked year.
   - Fixed: the sign lines of the LINEAR-GRAPHIK (`a18_lin`) stood at `i * nb((360 / w4d) * PI / 6)`, the first folded boundary times the sign number, so under a base angle below 360° only multiples of one position were tried and under 45° only the line at 240° fell inside the band. The port folds every boundary on its own, under 45° the lines stand at 240° and 120°, see `test_render.cpp`.
   - Fixed: VORHERGEHENDES Intervall of the LINEAR-GRAPHIK (`a18asw1`) went `jdbeg - 31` days back and kept that month for the one month window, from the first of March the window opened on the first of January and skipped February. The port opens every window on the first of the month the step names, see `test_directions.cpp`.
   - Fixed: the Septar offset of the Rhythmenlehre (`a174init`) was `sn = (sen - 1) * vp` in either unit. A Septar walks twelve houses, vp years in the month unit but twelve vp in the year unit, so with one year per house the third Septar dated its walk from the second year of life instead of the twenty fourth. The port takes `(sen - 1) * vp * 12 / fm`, see `test_symbolic.cpp`.
   - Fixed: the LJ and MO columns of the Rhythmenlehre split a negative age badly. The table (`a175`) took the years with `INT(sn + lj)` and the months from `FRAC(lj)` alone, minus two years three months printed `-3 -3.0` and a Solar of the age 35 read 35 minus 2.25 years as `32 -3.0`. The graph (`a178`) carried a rounded twelfth month toward zero, -2.99 years read `-1/ 0`, and the table never carried, 2.999 read `2 12.0`. The port splits sign and magnitude of sn plus the age and carries both, see `test_symbolic.cpp`.
   - Fixed: the date defined Sonderpunkt (`a17sonderpkt`) measured `lpkt` from the moment of the chart on screen and then subtracted `sn` again, a Solar of the age 30 put its point thirty years early and mostly outside the walk. The port inverts the clock of `a174init` exactly, see `test_symbolic.cpp`.
   - Fixed: the AUSLÖSUNGS-TABELLE of the Rhythmenlehre ran its direct loop to `np&`, twelve without extra bodies, and so never listed AC and MC, while the graph ran to eighteen and did. The port walks one list for both, see `test_symbolic.cpp`.
   - Fixed: `a17eing12` carried `CASE 8` twice, the SONSTIGE input and the abort, so ABBRUCH of the period box went on with the old period. The port ends the run, see `test_rhythm.cpp`.
   - Fixed: a date defined Sonderpunkt less than one phase before the birth gave `FIX(lpk) = 0` with a negative fraction, and `a17sonderpkt` extrapolated a degree below the first cusp. The port has no degree before the walk, see `test_symbolic.cpp`.
   - Fixed: the mundane GRAD-DATUM-LISTE (`a17_3` with `a174`) printed the projected degree `gr(i&)` beside each date and matched the Gruppenschicksals-Grade against it, so the rows named degrees the list never walked and the planet pairs fell on the wrong rows. The port prints the ecliptic half degree it dated and marks the published pairs there.
   - Fixed: `a17sol` compared `RIGHT$(iv$,2)` with "AR", but `iv$` ends in a blank, so the life span line of a Septar never showed, and its start lacked the unit factor. The port shows it with the corrected offset.
   - Fixed: ZEIT VARIIEREN (`zeitwi` behind `a9zw`) saved the varied slot with `merk` but restored with `merkr(1)`, so ending the walk on a SOLAR slot copied the solar record into the RADIX. The port restores the slot of the table on NEIN, and JA writes the varied moment into the RADIX set of the same number as his question VARIIERTE ZEIT in RADIX ÜBERNEHMEN ? says, see `test_coord.cpp`.
   - Fixed: the Wahr rows of the node and the Black Moon in PLANETEN-KOORDINATEN (`ko_ta`) ran `moko` after `etut` had set `jd` back to UT, while the chart itself reads the Moon at ET. Today the gap stays under an arc second, around -1000 it reaches a few thousandths of a degree for the node and more than half a degree for the true apogee. The port shows the ET values of the chart, see `test_chart.cpp`.
   - Fixed: the own point chooser of ARABISCHE TEILE (`arabt`) dispatched on the fixed list rows 35, 36 and 37 for HAUS NR., HERR v. HAUS NR. and EKLIPTIK-GRAD, which only matched with sixteen active extra bodies. Otherwise these rows fell into `CASE 19 TO 34` and stored a phantom body slot. The port dispatches by the meaning of the row and flags old records of that kind as invalid, see `test_arabic.cpp`.
   - Fixed: `art12` gave row 24 the additive form of Vermögen,Geld on a night birth whatever stood there, an own point on row 24 computed base + A + B. The port keeps the exception to the built in point.
   - Not a bug: paging the Sun table of INGRESSE (`ingre`) adds 365 days to `jd`, which `ingre1` leaves on the Pisces ingress of the shown year through its `@juld` after every row, so the step always lands in the next year. The port opens the next calendar year directly and shows the same tables, see `test_coord.cpp`.
   - Fixed: BEGINN HOROSKOP of VORGABEN HOROSKOP (`avh`) cleared the own degree `begz$` before it read the answer, so EXIT or R on that question left `begz&` = 5 with an empty degree and the wheel fell to zero Aries. The port clears the degree only when another beginning is chosen, see `test_avh.cpp`.
   - Fixed: the ASPEKT-LINIEN screen (`avh13`) forced Teiler 12 on every row past the fourth, the mode and exit rows included, so WAHLENDE could never leave a Teiler 4 or 8 behind. The port asks it only of the aspect rows 5 to 19, and names a row after its last `=` where his `RIGHT$(all$,lg&-1)` cut the row at the wrong end, see `test_avh.cpp`.
   - Fixed: ORBES der GRUND-ASPEKTE (`orbis_asp`) ran `CLR i&` before `et$(i&) = ""`, so a refused orb cleared nothing and the rows typed after it were lost on the redraw. The port shows the table again with every entry as typed, and ALLE auf 100 % of `orbis_pla` now resets the extra bodies too, which his loop to 15 left alone, see `test_avh.cpp`.
   - Fixed: the MULTI midpoint list (`halbsm`) walked the sign boundaries from `aa&`, one without a fixed point, so a directed midpoint at 0 degrees Aries never matched and only the 180 degree end of the axis did. The port tests all twelve boundaries, see `test_harmonics.cpp`.
   - Fixed: the MULTI and HARMONICS comparison grids (`a12asp` under `mult!`) wrote R over the directed body and M over the radix body. The port writes the tags the right way round.
   - Fixed: `zeitwim` declared `m!` LOCAL and never set it, so the H, M and S keys its legend offered did nothing. The port switches the unit as the legend says, see `test_avh.cpp`.
   - Fixed: the COMPOSIT AC and MC points (`a13`) were meant to turn to the side of their cusps, the flip code stands there for both, but `plz(0,1,13) = pl(13)` and `a92` wrote the unflipped midpoints back. Ascendants at 5° and 200° have their near midpoint at 282.5°, beside the MC midpoint 285°, while the ROBERT HAND first cusp stands near 33°. His final program showed and aspected the AC at 282.5°, the port keeps the flip and puts it at 102.5°, see `test_chart.cpp`.
   - Fixed: the COMBIN place (`a14`) took the plain mean of the longitudes, `gl = glc / z&`. Places on both sides of the date line met on the far side of the globe, 170° east and 170° west gave 0°, Greenwich, where 180° lies between them, and three at 170° E, 170° W and 160° E gave 53.3° instead of 173.3°. The port cuts the circle in the widest arc free of places and averages there, which is his plain mean for every group that leaves the date line alone, see `test_chart.cpp`.
   - Fixed: ERGEBNIS als RADIX (`erg_rad`) ignored the *Irrtum ( = UNDO = Zurück )* answer of the overwrite question. `a2113_1` set `expr!`, but `erg_rad` went on and wrote the result over the slot anyway. The port leaves the slots as they were, see `test_diverses.cpp`.
   - Fixed: the ecliptic latitudes carried the nutation in obliquity, `eb(1) = -heb(1) + deps`, `eb(g&) = deps + ASIN(...)` and `eb(2) = deps + ...` in `moko`. Nutation in obliquity turns the equator, the ecliptic latitude stays (Meeus ch. 22 and 47), so every latitude and the declinations built on it were off by up to 9.2″. For the Meeus Moon example of 1992-04-12 the original gave about −3.2265° where Meeus prints −3.229126°. The port leaves `deps` in the obliquity alone, see `test_sunmoon.cpp`. The osculating true node is built on that latitude and moved with it, in 25 of the 50 golden charts by more than the original's printed minute allows. Against the Swiss Ephemeris osculating node the port's mean error fell from 60.9 to 17.6 arc seconds over the 26 charts the fix moved by 30 arc seconds or more, the original itself sits about 56 off, see `tests/golden/README.md`.
   - Fixed: the true apogee latitude (`moko`, `eb(nk&(1)) = ASIN(SIN(u4) * SIN(i4))`, "Breite des Knotens") used u4, the argument of latitude of the Moon itself, so the Black Moon inherited the latitude of the Moon and its declination could be degrees off. The port takes the apogee's own argument of latitude `u4 - wa + π`, the angle `el24` rests on, and meets the latitude of the negated eccentricity vector of the same state, see `test_chart.cpp`.
   - Fixed: the aspect and mirror scans (`asp1`, `spieg1`) took a position of zero for an empty slot and `w3 > 0` for a real distance, so a body at exactly 0° Aries, the fixed point typed as 0 Aries or a 90 degree dial position folding onto zero never aspected, and two bodies on the same degree never formed their conjunction. `stelk` dropped a body exactly on the star the same way, and the sign columns of the histogram (`elem1`, `kard_fix_gem`) lost a body at 0° Aries or on a sign boundary between their kk edges. The port asks the slot whether it is filled and counts the exact places, see `test_aspects.cpp`, `test_chart.cpp` and `test_histogram.cpp`.
   - Fixed: the STATISTIK list (`usuch`) compared `VAL(RIGHT$(d$))`, the last digit of a chain, with the number of the condition. From the tenth condition on no chain counted as complete, the count of the records meeting all UND conditions read zero and no row turned inverse. The port reads the whole number after the last chain mark, see `test_statist_list.cpp`.
   - Fixed: the STATISTIK rows (`list_ausg`) printed `mz%(m%)`, the value the record took last, on every entry of that record. After ODER or under UND the row of the first condition showed the position of another object. The port prints the value of each entry.
   - Fixed: the STATISTIK NAME search (`bed_erf_nam`) compared the typed letters with the upper case field as typed, a search in small letters never found anything. The port compares in capitals, see `test_statist_list.cpp`.
   - Fixed: the PLANETAR periods (`a16_ta`) of Jupiter to Pluto ran too long, Saturn 10821.4 days where the sidereal period of its mean motion is 10759.2 (Meeus Table 31.A), Jupiter, Uranus, Neptune and Pluto likewise. The DATUM box showed these periods and counted with them, so a Saturn return late in life read one number low. The port counts with the sidereal periods, see `test_transit_search.cpp`.
   - Fixed: the PLANETAR DATUM count (`ns1& = FIX((jd - jd(1,ze)) / ta(pl&))`) divided the geocentric Mercury and Venus by their revolution of 88 and 225 days while his NR branches seed them once a tropical year, so the same return read 10. by number and 41. by date. FIX also truncated toward zero, a date before the birth lost its minus one. The port counts the geocentric inner two by the tropical year and floors, see `test_transit_search.cpp` and `tests/gui/test_auswertung.cpp`.
   - Fixed: the record list print (`datei_pr`) forced a ninetieth of the printable width on every character of its bold fixed font, the rows of his record list run to about 112 characters and were cut near the 85th, and the first page began with the empty index 0 of the list. The port narrows the pitch until the longest row fits and starts with the first record, see `test_hardcopy.cpp`.
   - Fixed: the DOPPEL - DARSTELLUNG box of `mehrf_1` read "ausdruckenm indem", the port prints the word he meant.
   - Fixed: GROSSES JAHR (`grossj1`) compared the Pisces start of 360° unfolded with the normalised precessed longitude, `di = (la1 - la) * up` came out as 358.6° instead of -1.4° a century after the reference, the `NICHT MEHR im FISCHE - ZEITALTER` warning fired on every date and only the display folded the difference. The port folds `di` into (-180°, 180°] and keeps his two tests, before the reference or more than a sign behind it, see `test_chart.cpp`. His first box also printed `Zeitalters-Punkt : 30°` for every age, the port names the chosen start.
   - Fixed: the clock text `homi$` of the FINSTERNISSE rows rounded the minutes with `STR$(x,3,0)` and carried no hour, a moment of 11:59:40 printed as `11h  60m`. The port rounds the moment to the minute first, see `main_window.cpp` `eclipse_table`.
   - Fixed: FINSTERNISSE (`finst_0`) carried `0.1017438` for the T² term of the moon's mean anomaly where Meeus prints `0.0107438`, his other terms of that line match the same edition. The error grows with T², the new and full moons of the year 1000 came out up to 86 minutes away from the conjunction found on the port's own VSOP87 sun and ELP moon, around year 0 some eclipse flags change. The corrected series meets them within ten seconds, see `test_chart.cpp`.
   - Fixed: the zone box read the zone of a picked place with his `FUNCTION VAL`, which keeps only `+ , - .` and the digits of the last five name bytes before GFA's VAL. Where a name abbreviated with a dot runs up to the zone, as in "Bialystok / Pol.  -1" or "Jerusalem / Isr.  -2", the kept text starts ".-1" and VAL read zero, so the pick set UT. Fourteen places were hit, eight in EUROPA.INT and six in WELT.INT, among them Jerusalem, Peking and Perth. The port reads the trailing signed number, see `test_data.cpp`.
   - Fixed: his shipped place lists carried seven wrong records, found by checking every zone against its longitude and known geography. EUROPA.INT had Saloniki and MADRID at +2 (Greece is UTC+2, zone difference −2, Spain UTC+1, −1), Tirana at −2 (Albania keeps CET, −1), Coimbra at 8.43° east (it lies at 8.43° west), Cambridge at 5.85° east (0°07′ east) and Powderham at 53.47° N 1.57° E, the longitude of Beccles (Powderham lies at 50°38′ N 3°27′ W). WELT.INT had Kapstadt at +2 (South Africa is UTC+2, −2). A pick from these lists set a zone off by up to four hours. The records are corrected in `data/places`, see `test_data.cpp`.
   - Fixed: the zone box `zeitzon` of the NEU-EINGABE wrote -2 for EINFACHE SOMMERZEIT and -3 for DOPPELTE SOMMERZEIT whatever zone the field held, right only for MEZ. Athens picked from the EUROPA list and marked DSZ still gave -2, one hour short. The port adds the summer hours to the chosen zone, see `test_entry.cpp`.
   - Fixed: KORREKTUR by sidereal time, MC, AC or an intermediate cusp (`korr21`) put the wanted ARMC on the UT date of the birth. The same ARMC returns every sidereal day, so a birth shortly before midnight UT came out almost a day early, 23:50 UT with the MC moved 3° gave 00:06 of the same date instead of 00:02 the next day. The port takes the moment nearest the birth, see `test_rectification.cpp`.
   - Fixed: the cusp walk of `korr1` tested `ABS(ab1 - ab) > 2.5` on two normalised angles without folding, a walk from 2° to 355° counted as a jump of 353° and turned the result by π, twelve hours away. The port folds the difference first, see `test_rectification.cpp`.
   - Fixed: KORREKTUR by the Moon seeded the search at `jdx + tmo·(el − pz)/2π`, on the far side of the birth, and its repairs `jd − jdz > tmo` never fire after a backward search. A target more than about 33° ahead of the natal Moon came out one lunation early. The port takes the crossing nearest the birth like the year repair of the Sun, see `test_rectification.cpp`.
   - Fixed: `prima` turned the directed ARMC by `dif·360/tja`, 0.9856° per degree of sidereal time variation, while its take over moved the birth by `dst` days per degree, so the preview and the corrected radix disagreed. Both turn the ARMC one degree per degree now, see `test_rectification.cpp` and `test_korrektur.cpp`.
   - Fixed: `prima3` printed BISHERIGE GESAMT-DREHUNG as `brm + dif − arm` although `brm` already held the variation, a 2° variation read as the arc plus 3.971°. The port prints the arc plus the variation, see `test_korrektur.cpp`.
   - Fixed: AUFGANG tested the polar limit against the obliquity of the loaded chart, not of the entered date as `juld` leaves it, for 21.6.1600 the port refuses at 66.508° where a 1999 radix allowed 66.56°. See `test_diverses.cpp`.
   - Fixed: `zeitwi` advanced `jd` twice per frame, at `zw1` and again before the redraw, so every screen of ZEIT-WANDERN stood two steps on. The port walks one step per frame, see `test_wander.cpp`.
   - Fixed: the comparison scan `a12asp` demanded `w > kk`, `wa1 > kk` and `wa2 > kk`, an exact aspect and every body at exactly 0° Aries dropped out and a DOPPEL-KREIS of one record lost all its conjunctions. `aspar` dropped a body at 0° Aries from the ASPEKTARIUM the same way. The port asks the slot, see `test_aspects.cpp` and `test_render.cpp`.
   - Fixed: the ASPEKTARIUM printed `STR$(60 * FRAC(di),2,0)` beside `FIX(di)`, so 10°59.8' read 10° 60'. The port rounds the minute and carries it, see `test_render.cpp`.
   - Fixed: `mc_armcb1` has no CASE for MONDKNOTEN S, which the BEZUGS-FAKTOR box offers, MULTI 3 and MULTI-ARC then ran from e = 0, Mars at 200° under MULTI 3 at age 2 went to 40° instead of 260°. The port reads the south node, see `test_harmonics.cpp`.
   - Fixed: `a901_m` read `ta_na` from the working arrays after `multi11` had zeroed `pl(13)`, so MULTI 1 always took the night formula of the Glückspunkt and the other modes set the radix Sun against the directed AC. The port keeps the day or night rule of the birth, see `test_harmonics.cpp`.
   - Fixed: `a180_1` chose the direct or the retrograde bracket test by the order `vergl2` left the pair in. A retrograde step across 0° Aries read as a direct step of almost a full circle and the speed cap dropped it, so no retrograde body ever transited a point just around 0° Aries and the mean node never reached the Aries point. The port picks the branch by the mean daily motion of the interval, see `test_transit_search.cpp`.
   - Fixed: `a180` reset `pl1&` to `aa&` for SEKUNDÄR, SONNEN- and MOND-BOGEN after `a18eing_plw` had asked, so Ab MARS and Ab JUPITER AUFWÄRTS changed nothing and the lists still named the directed Sun to Venus. The port starts at the chosen body, see `test_transit_search.cpp`.
   - Fixed: `stat_ausw` IN HAUS with an orb took `dw = (w2 - w1) * obp / 100` from the raw cusps. For a house over 0° Aries this went negative, cusps at 340° and 10° with 10 % gave −33° and searched 13° to 337°, the rest of the circle. The port folds the width first and searches 337° to 13°, see `test_statist_eval.cpp`.
   - Fixed: `stat2` took AC, MC and house 2 at zero as a heliocentric file, a dataset of house system 9 or 10 switched the program to HELIO. The port also requires the Sun at zero, which only the heliocentric writer of `stat1` leaves, see `test_statist_eval.cpp`.
   - Fixed: `stat2_teil` named the .STH after the first seven letters. Two such datasets shared one twin, the second overwrote the first's extras and deleting one removed the other's. The port gives a colliding dataset its whole base name and spares a shared twin, see `test_statist.cpp`.
   - Fixed: VERWERFEN in `stat1` built the .PAR name from an unset `l&` and killed only the .PAR, leaving the torn .STA and .STH behind. The port removes all three.
   - Fixed: `stat2` checked `FRAC(LOF(#26) / 24 <> 0)`, FRAC of a comparison, which never fired. The port checks the .STH record size.
   - Fixed: `a22dat` compared `TRIM$(na$)` case sensitively with the upper case field, so a name not typed in capitals was appended twice, and the append wrote the .STH at `RECORD #26,k&` with a stale `k&`. The port compares without case and appends both files together.
   - Fixed: `obj_wahl` built the ASPEKT mit HALBSUMME label twice and with `pl$(1)`, the Sun, as the first factor. The port names the chosen factor once.
   - Fixed: `such_wo` meant OHNE EINSCHRÄNKUNG for the first condition only, but its ELSE IF chain stopped at the angle and cusp test, so an angle, cusp or HAUS NR. as a later condition could still pick it and run the whole file past UND. The port greys it.
   - Fixed: `grze` carried only the rounded seconds and rounded the minutes with CINT afterwards, so gz0$, gz1$, gz8$ and the STATISTIK rows could read 60 minutes, "15° LE 60'" for 15°59'40". The port rounds once at the last unit shown and carries into the degree and the sign, see `test_coord.cpp` and `test_stat_sheet.cpp`.
   - Fixed: `horcom_aaf3` split coordinates with FIX and CINT without a carry and could write a sixtieth second, 11°59'60". The port carries into minutes and degrees, see `test_data.cpp`.
   - Fixed: `a2113_1` stopped the running clock only when it sat in the fifth slot (`zeuhr = 5`), a clock in a slot that gave way went on writing its moment into the new record. The port stops the clock exactly when its own slot gives way, see `test_records.cpp`.
   - Fixed: LÖSCHEN Beenden ran `aaf_horcom2` even after AAF-Datensätze BEIBEHALTEN, so every deleted record came back into the DAT. The port rebuilds the DAT only when the AAF lost the same records, see `test_entry.cpp`.
   - Fixed: `aaf_ident` took the first record matching either of its rules, so "MAIER *" won over the exact "MAIERHOFER ANNA" further down. The port searches the exact name through the whole file first, see `test_data.cpp`.
   - Fixed: the LUNAR-LISTE of `a16` called `a16_1` after the BEGINN-DATUM box, which wrote the birthday's day and month over the entered ones, so the list always opened near the birthday of the entered year. The port starts at the entered date with his `SUB jd,29` and `ADD jd,33`, see `gui/test_auswertung.cpp`.
   - Fixed: the `gs&` list of `a17_3` lacked half degree 214 (107°, MO-SA) that `a174` draws, so the own degree definition called it "NICHT von W.DÖBEREINER". One list serves both, see `test_rhythm.cpp`.
   - Fixed: `solnummer` numbered a return from its search seed with five days of slack. A LUNAR searched by date lies up to a lunation past its return, so a date in the last four days before the next lunar took that next number. The port counts whole periods from the birth to the return found, see `gui/test_auswertung.cpp`.
   - Fixed: FIX-STERN-POSITIONEN compared AC and MC with the stars only while extra bodies were on, `np& = 12` ended the plain loop at the south node while `a18st` let the extra run pass 13 and 14. The port always takes the axes and, like `stella` with `aa& = 0`, the fixed point.
   - Fixed: the ASPEKTARIUM of `aspar` wrote `@textc(510,435,16,"Zusatz-Planeten:")`, sixteen characters of ten units from 510, past the 640 edge of his own screen. The port sets it in the 13 of the `Planeten-` and `Gewichtung:` lines beside it and ends it at 622, see `tests/gui/test_layout.cpp`.
   - Fixed: the STATISTIK list (`kltext`) stood its rule at `x% + 96`, inside the tens of a two digit hour, his moment of eleven date cells, two blanks and the hour cut every time from 10 h on. The port stands the rule in the two blanks at `x + 84`, see `tests/gui/test_layout.cpp`.
   - Not a bug: the MITTEL of `hubausg` sums the samples 0 to 6000 and divides by 6000, the time mean over fifty years of 120 samples each, as the port does, see `test_transit_search.cpp`.
   - Not a bug: the Dynamogramm's `IF NOT pl& = rd& && na& = 1 //Radix-Werte selbst ausschalten` means `NOT (pl = rd && na = 1)` under the same rule, every aspect counts and only a body's conjunction with its own radix place drops out, as the port does.
   - Not a bug: Halley read as B1950 through the duplicated `CASE n2&,n18&`. The generator listing says J2000, but the shipped `halley.eph` meets his B1950 start elements of `elem_halley` only when read as B1950 (0.04° against 0.71°), so the reader was right, see `test_eph_file.cpp`.
   - Not a bug: the `ORT.EXT` writer `ortp` tests `IF NOT EXIST(...) = TRUE`, which reads `NOT (EXIST(...) = TRUE)` under GFA's NOT rule, so it writes the preferred place only while the file is missing, and every caller that replaces the place KILLs the file first. The port asks and writes in one step, see `test_places.cpp`.
   - Not carried over: `reg_resdat` restores the backup after a failed write, for the place files from `SPEZ_ORT\RRESERVE.DAT` while `make_resdat` wrote `SPEZ_ORT\RRESERVE.INT`, so a place file was never rescued. The data layer writes every file atomically, there is nothing to restore.
   - Kept: `textg` overlapping width ranges (19/20), the first CASE wins and gives both heights the width 10, which is what his screen showed, `font_pitch` does the same.
8. **Personal data** never enters the repo (CLAUDE.md). Loaders are tested with synthetic fixture files, not with files from `legacy/`.
9. **Printing keeps his DRUCKER-OPTION model.** His final KONSTA7P.INT had the option on for the printer at his desk, a per machine preference rather than program logic. The shipped profile has it off, so no output stops at a HARDCOPY box nobody asked for, while DRUCKEN and the PDF export print at any time and F8 or DRUCKER-OPTION EIN / AUS switch it on for good. While it is on, the output windows of his menu offer the HARDCOPY box on the keys that left his `wart`, HOROSKOP-GRAPHIK, the paired charts, the SOLAR and LUNAR tables and the linear graphic ask `druck_graph_ein` first, and every print passes `druck_einr_anz`. The output windows carry their `muuu&` number as a property, one hook in the application key filter serves them all and replays the key to the output after the HARDCOPY. Pages print through the shared painter at his VIEWPORT and StretchBlt geometry (`apps/gui/print_pages.*`). The date and time of `drad2` go onto printed pages only, the screen and the SVG export stay deterministic for the golden comparisons. `set_print_file` sends the prints of the tests into a PDF instead of the system dialog, and the GUI suites switch the option off unless a test drives the printer.

10. **The running rings of `a20_horg` ride on the shell's views.** Transits use the transit view, the secondary and solar arc rings the arc view, the `zeitwi` walk reuses the wander view and counts as one Back step. The factor choice of `a18eing_plw` holds during the walk, afterwards the ring shows every factor again. The ring of the chosen moment already stands behind the ZEIT-EINHEIT box. The walk shows his `drgrph!` screen in full (`MainWindow::a20_sheet`, checked against his own web page screenshot of a TRANSITE walk): the `bes11` columns of the radix at x 2 and of the running chart at x 112 headed `Transit:`, `Progress.:` or `SO-Bog.Dir:`, each with the cusps of `bes111`, the `a12asp` grid below them with the running body first and his one degree orb, the rule of `bes1` at 220 with the name beside it, the `Ereig.-Ort` of `bes111` and the moment corner, and the running AC and MC on the outer ring as his AC and MC sprites like every factor of `plein1(212)`. The TRANSITE walk keeps that screen until the transit view is left, the Transite switch of the panel keeps its own modern view. The running column lists only the chosen factors like `a18st`, and the grid starts below the longer of the two columns where his `yt&` ran on from the running one. `primhorg`'s lines under the wheel sit in the lower left rows because his coordinates would fall off the screen sheet, and MUNDAN asks its place through the `ort_wahl` box instead of his input mask.

11. **The screen comparison reads his web pages as 5P and 6P.** The screenshots on his own web pages come from HORCOM5P and 6P, so every difference is settled against the 7P listing and, where the listing is not enough, the running HORCOM7P under otvdm. Differences that are 7P behaviour and stay: the house of the running phase arced alone in the rhythm wheel (`a1795`), the aspect chords of `asp1` in the transit and MULTI wheels, the mundane positions of `a17_3` in the GRAD-DATUM-LISTE under the mundane frame (his 6P list was still linear in ecliptic degrees), the bare left panel of HOROSKOP-GRAPHIK without the 5P histograms, the whole date and time inverted by `tinv` in the eclipse table, the multi body first in the MULTI grid with the tags written the right way round. Settings differences are not findings, the true Black Moon, the mean node and the extra bodies of his screenshots. Fixed from the comparison: every sheet printed the record label where his `sol$(od,ze)` names the kind of chart (the fixed star and Arabic parts footers, the GRAD-LISTE title, `hsa0`, the `a18kopf` name line and the linear graphic), the a18 lists open with LISTE SORTIEREN ? like `dirend`, `stelk` sets the aspect symbol before the body tag, the classic sheet lists the `spieg1` pairs, and the result tables carry his symbol before each body tag. The tables stay tables, a documented modernisation, brought closer to his screens where they fell short: the coordinate and rise/set screens grow their text with the window like his full screen sheets (`TableZoom` in `apps/gui/table_fit.hpp`, a bisection because the notes that grow along take room from the table), and the GRAD-DATUM-LISTE draws the planet pairs as his two boxed sprites of `a174g` (`SpriteRowDelegate`). Every table cell keeps a text margin, the item rules of the style sheet had set it to zero.

## 4. Protected logic (CLAUDE.md "Preserving Robert Rettig's engineering")

The cluster `par` (topocentric parallax, Montenbruck p.25 formulation in equatorial coordinates), `ko_tr1`/`ko_tr2`, `praez`, `plkoap` (L49393–49493) and the osculating-orbit true node/Lilith in `moko` are ported faithfully first and verified against the original program's printed coordinates before any change is even proposed. The parallax path notably includes the `par = 1` variant of the transit search (`plant1` recomputes sidereal time per step so the topocentric correction tracks the event location). When old and new disagree, the original is presumed right.

## 5. Verification strategy

1. **Golden values from the original.** `HORCOM7P.EXE` (16-bit) runs under winevdm/otvdm or a 32-bit VM; the GFA IDE (unpacked from `legacy/GFAWIN`) can also run the source directly. For a grid of test epochs (spanning Julian/Gregorian, BC years, the .EPH coverage bounds, DST cases) we record the program's planet coordinate tables (menu 37/38), house tables (menu 80), and parallax on/off pairs, and store them as fixture files.
2. **Data-derived checks.** The `.EPH` binaries themselves are ground truth for the reader; `PLANETS.DAT` record 0 (Earth L0 = 1.75347045673) and friends pin the VSOP loader.
3. **Property tests** for the kernel (normalisation, atan quadrants, calendar round-trips, interpolation continuity at record boundaries).
4. Each module lands only with its tests; nothing merges on visual inspection alone.
5. **Layout checks.** `tests/gui/layout_check.hpp` measures what a screen shows. On a drawn sheet it finds texts leaving the paper, texts running into each other and column rules through a line of text, with the painter's own `text_box`, and it honours his opaque grounds and the blanks between the fields of a fixed pitch line. On widgets it finds table columns and rows narrower than their delegate asks, headers, labels and buttons narrower than their text, and a dock that cuts a column. The `layout:` cases of the GUI suite run as their own ctest `horcom_gui_layout` on the offscreen platform with a full HD screen from `tests/gui/layout_screen.json`, on Windows pointed at the system fonts, since the plain offscreen platform of Windows draws every letter as an empty box and its measurements mean nothing. With `HORCOM_SHOTS` set to a folder, the GUI suite captures and checks every dialog its flows open, the sweep that audits all screens at once, with a PNG and the findings beside it for each.

## 6. Phases

| Phase | Deliverable |
|---|---|
| 0 | Toolchain (compiler + CMake + test framework), CI-less local test run |
| 1 | `core` + `time` (constants, angles, JD, ΔT, sidereal time, equation of time) with golden tests |
| 2 | `ephem` (VSOP loader/eval, Moon, Kepler, .EPH reader, nutation, precession) — planet longitudes match the original |
| 3 | `chart` calculation (geo conversion, apparent, parallax, houses, aspects, midpoints, special factors) — full coordinate + house tables match |
| 4 | `data` + `geo` (legacy loaders, AAF, settings; timezone seed data) — existing user files open |
| 5 | `render` display list + PNG/SVG export — the wheel reproduces the legacy layout (radii table in ui-and-graphics.md) |
| 6 | `app` GUI shell (framework decision due here), transits/directions/returns screens |
| 7 | Extras: statistics module, Münchner Rhythmenlehre/Huber, primary directions, eclipse finder, clock chart |

## 7. Toolchain

Target: C++20, CMake ≥ 3.25, warnings-as-errors, `doctest` (vendored single header) for tests. MSVC 2022 is the primary target on Windows. Linux builds with GCC ≥ 12 and Clang ≥ 18 under `-Wall -Wextra -Wpedantic -Werror` and `-ffp-contract=off`, so no compiler fuses Robert's multiply-add steps and the numbers stay byte identical to MSVC (verified over 600 randomised charts). CI runs GCC, Clang and an ASan/UBSan build on Ubuntu 24.04 and packs the release AppImage on Ubuntu 22.04 with Qt 6.8.3 (`packaging/linux/appimage.sh`). On a read-only install (AppImage, `/usr`) the GUI works in `~/.local/share/horcom/data`, seeded from the shipped data, while planet tables and ephemerides stay read from the install.

## 8. References Robert Rettig worked from

Jean Meeus, "Astronomische Algorithmen" (VSOP87 abridgement, Moon, nutation, mean elements — his own citation is preserved in `data-formats.md` 8.2). Oliver Montenbruck, "Grundlagen der Ephemeridenrechnung" (parallax p.25, node/orbit geometry p.75/78, precession p.18/33, Halley p.165). "Coordinates of the Outer Planets 1653–2060", Astronomical Papers Vol. XII, Washington 1951 (the Störmer method, which he cites down to the Staatsbibliothek shelf mark). Guthmann (RK6, p.256). Chapront (Pluto series). P.D. via B. Mahl (timezone tables), B. Mahl (Arabic parts). The Aramis GFA-to-C++ converter in `legacy/HORCOM/GFA/` is not used for the port (it generates BASIC-emulation wrapper code) but may serve as a semantics reference for GFA constructs.
